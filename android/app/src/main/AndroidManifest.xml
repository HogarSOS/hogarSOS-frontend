<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Requerido en Android 13+ (API 33) para mostrar notificaciones —
         sin este permiso, firebase_messaging recibe los mensajes pero el
         sistema nunca los enseña en la barra de notificaciones. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
    android:label="Hogar SOS"
    android:name="${applicationName}"
    android:icon="@mipmap/ic_launcher">
        <!--
          Clave de Google Maps restringida en Google Cloud Console
          (proyecto "hogarsos") a Maps SDK for Android + huellas SHA-1
          release y debug del paquete com.example.hogarsos.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDcipMwcfNXNh9k7FoIodM4aF0YJtcpJnI" />
        <!-- Canal de notificación por defecto para firebase_messaging en
             Android 8+ — sin esto, el sistema usa uno genérico sin icono
             ni nombre propios. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="hogarsos_notifications" />
        <!-- Impeller (el backend de render por defecto en Android desde
             Flutter 3.x) se queda colgado sin dibujar ni un frame en el
             Realme RMX3853 de pruebas (SurfaceView "didn't commit buffer"
             en logcat, indefinidamente) — problema conocido de
             compatibilidad Impeller+Vulkan con ciertos drivers Adreno.
             Skia es más lento en algunos efectos pero no se cuelga aquí. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link propio para volver a la app tras el onboarding
                 hospedado de Stripe Connect (ver return_url/refresh_url en
                 professional.controller.ts y la página de retorno en
                 stripeOnboarding.routes.ts). android:autoVerify no hace
                 falta: no es un App Link https verificado por dominio,
                 es un esquema propio (hogarsos://), que Android abre
                 siempre con esta app sin necesidad de verificación. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="hogarsos" android:host="stripe-return"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
