<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme.MaterialComponents.Light.NoActionBar, no un tema Android
         plano: flutter_stripe exige explícitamente un tema AppCompat o
         MaterialComponents para el Payment Sheet nativo — con un tema
         plano, presentPaymentSheet() fallaba SIEMPRE con
         "flutter_stripe initialization failed" (mensaje real: "Your
         theme isn't set to use Theme.AppCompat or
         Theme.MaterialComponents"), antes incluso de abrir la hoja de
         pago. La clase ya está disponible sin añadir ninguna
         dependencia nueva: stripe_android trae Material Components
         transitivamente. -->
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@style/Theme.MaterialComponents.Light.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@style/Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@color/hogarsos_splash_background</item>
    </style>
</resources>
